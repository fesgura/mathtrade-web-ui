const validations = {
  version_name: ["required"],
  language: ["required"],
  publisher: ["required"],
  year: ["required"],
  dependency: ["required"],
  status: ["required"],
  //comment NO REQUIRED

  //item_title = name

  //item_id
  //type
  //bgg_id
  //thumbnail
  //bgg_version_id
};

export default validations;
