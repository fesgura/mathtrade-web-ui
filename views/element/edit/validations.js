const validations = {
  version_name: ["required"],
  language: ["required"],
  publisher: ["required"],
  year: ["required"],
  status: ["required"],
  //comment NO REQUIRED
  //dependency  NO REQUIRED ,

  //item_title = name

  //item_id
  //type
  //bgg_id
  //thumbnail
  //bgg_version_id
};

export default validations;
